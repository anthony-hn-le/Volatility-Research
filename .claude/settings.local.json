{
  "permissions": {
    "allow": [
      "Bash(pip3 --version)",
      "Bash(python3 -m venv .venv)",
      "Bash(.venv/bin/pip install *)",
      "Bash(.venv/bin/pip freeze *)",
      "Bash(.venv/bin/python -)"
    ]
  }
}
